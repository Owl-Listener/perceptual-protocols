# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Environment — never commit API keys
.env
.env.*
!.env.example

# Smoke-test artefacts from running generate_mood.py at the repo root.
# Leading slash scopes these to the root only, so committed examples
# like examples/folk-maximalism/mood.md and its mood/ folder are kept.
/mood.md
/mood/

# OS / editor
.DS_Store
*.swp
